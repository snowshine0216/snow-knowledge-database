---
tags: [claude-code, geektime, ai-agents, sub-agents, parallel, pipeline]
source: https://time.geekbang.org/column/article/945070
---

## Pre-test

> *阅读前尝试回答以下问题。答错完全正常——预测试能让大脑在接触正确答案时编码得更深。*

1. 在 Claude Code 中使用多个子代理并行执行任务时，你认为最大的风险或前提条件是什么？
2. 如果要用子代理探索一个大型代码库，你会选择功能最强的模型（如 Opus）还是更轻量的模型（如 Haiku）？为什么？
3. 当多个子代理同时在前台运行时，你猜测如何避免终端被输出占满？

---

# 07｜百舸争流：多任务并行探索与流水线编排

## 章节元数据
- 课程名（EN）: claude-code-engineering-practice
- 课程名（ZH）: Claude Code 工程化实战
- 章节: 
- 作者: 黄佳
- 文章 ID: 945070
- 来源: https://time.geekbang.org/column/article/945070

## 康奈尔笔记

### 线索栏（问题）
- 本章的核心论点是什么？
- 本章开场提出了哪些背景、问题与动机？
- “场景一：新接手一个大型项目”这一部分的关键观点是什么？
- “场景一：新接手一个大型项目”有哪些支撑证据或案例？
- “主题3”这一部分的关键观点是什么？
- “主题3”有哪些支撑证据或案例？
- “项目一：并行探索”这一部分的关键观点是什么？
- “项目一：并行探索”有哪些支撑证据或案例？

### 笔记栏
#### 概览
- 你好，我是黄佳。
- 欢迎来到 Claude Code 工程化实战的第 7 讲。前面我们学习了只读型子代理和高噪声任务处理，今天我们要探索子代理的另外两个强大工程应用场景——并行探索和流水线编排。
- 这两个应用分别解决的问题是什么呢？并行探索,当你需要同时从多个角度理解或处理一件事时启用；流水线编排,当一个复杂任务可以拆成多个连续阶段时启用。
- 今天的内容比较多，学习目标包括：
- 掌握并行探索和流水线编排的基本使用。
- 通过实战项目体验两种模式的差异。

#### 场景一：新接手一个大型项目
- 你刚加入一个团队，需要快速理解一个包含几十个模块的后端项目。传统方式下，我们可能要花费很久的时间熟悉。
- 串行探索既慢，又容易在中途忘记前面看到的细节。
- 而并行探索的方式就不一样了。
- 三个子代理同时工作，各自探索自己的领域，最后汇总成一份综合报告。

#### 主题3
- 原始小节标题：场景二：修复一个复杂的 bug
- 遇到一个“用户登录后偶尔 token 验证失败”的 bug。这种间歇性问题最难调试。
- 如果让主对话直接处理，上下文会很快被塞满：
- 搜索相关代码 → 200 行输出
- 分析可能的原因 → 又是 200 行
- 修复 → 100 行
- 验证 → 又是测试输出……

#### 项目一：并行探索
- 接下来我们直接进入实战演练。

#### 并行探索子代理
- 实战项目位于 03-SubAgents/projects/04-parallel-explore/，包含三个专门的探索子代理。
- auth-explorer子代理的配置如下。
- 关键设计点如下：
- tools: Read, Grep, Glob，全部只读，探索不需要修改任何东西，这三个工具足够完成代码探索任务。
- model: haiku，探索任务相对简单，追求速度，haiku更快更便宜。
- Stay within auth domain：明确告诉子代理它的职责边界，防止它越界去分析不相关的代码。

#### 使用并行探索
- 进入项目目录，在 Claude Code 的命令行中输入：
- 此时 Claude 会完成下述步骤。
- 并行启动三个子代理
- 各自独立执行探索任务
- 收集三份报告
- 综合成一份整体架构理解

#### 前台与后台运行
- 在 Claude Code 中，子代理默认在前台运行——你能看到它实时输出的每一行。并行探索时，如果三个子代理都在前台，你的终端会被占满。
- 当一个子代理正在前台运行时，按 Ctrl+B 可以将它切换到后台继续执行。这在并行场景下非常实用。
- 我们梳理一下操作流程。
- 触发 auth-explorer → 看到它开始搜索
- 按 Ctrl+B → auth-explorer 转入后台
- 触发 db-explorer → 看到它开始搜索

#### 并行探索的隐含前提：任务必须真正独立
- 并行看起来很美好，但有一个容易被忽略的前提：各子代理的探索任务之间不能有信息依赖。
- 什么叫有信息依赖？我们拿一个电商项目举例。
- auth-explorer 发现用户认证使用 JWT，token 中包含 userId 和 role 。
- db-explorer发现 users 表有 role 字段，但 orders 表里也有 user_role 冗余字段 。
- api-explorer: 发现 /admin/* 路由用了中间件检查 role。
- 问题来了！这三个发现之间有关联——role 的传递路径横跨三个模块，但因为并行执行，每个子代理都不知道其他两个发现了什么！这不是 bug，而是设计约束。并行探索的综合分析必须由主对话来完成。子代理负责“收集原始情报”，主对话负责“连点成线”。

### 总结
今天我们要探索子代理的另外两个强大工程应用场景——并行探索和流水线编排。

## 关键要点
- 你好，我是黄佳。
- 场景一：新接手一个大型项目：你刚加入一个团队，需要快速理解一个包含几十个模块的后端项目。传统方式下，我们可能要花费很久的时间熟悉。
- 主题3：遇到一个“用户登录后偶尔 token 验证失败”的 bug。这种间歇性问题最难调试。
- 项目一：并行探索：接下来我们直接进入实战演练。
- 并行探索子代理：实战项目位于 03-SubAgents/projects/04-parallel-explore/，包含三个专门的探索子代理。
- 使用并行探索：进入项目目录，在 Claude Code 的命令行中输入：

## 知识图谱种子
- 实体:
  - claude-code-engineering-practice
  - 07｜百舸争流：多任务并行探索与流水线编排
  - 黄佳
- 关系:
  - (claude-code-engineering-practice) -> 包含章节 -> (07｜百舸争流：多任务并行探索与流水线编排)
  - (07｜百舸争流：多任务并行探索与流水线编排) -> 作者 -> (黄佳)

## 复习备注
- 构建知识图谱前，先复核关键论断与原文的一致性。
- 在此补充你的行动项、实践映射和复盘结论。


---

## Post-test

> *关闭文件，凭记忆写出或大声说出你的答案，再对照答案指南（费曼检验：无法简单解释，说明仍有理解空白）。*

1. 用你自己的话解释"并行探索的隐含前提"是什么，并用本课的电商项目案例说明违反这一前提会导致什么问题。
2. 在并行探索的子代理设计中，为什么选择 `Read、Grep、Glob` 这三个只读工具，并使用 `haiku` 模型？这两个设计决策背后各自的工程理由是什么？
3. 并行探索和流水线编排分别适用于什么类型的问题？用本课给出的两个场景（新接手大型项目、修复间歇性 bug）对比解释它们的差异。

> [!example]- Answer Guide
> 
> #### Q1 — 并行探索的隐含前提
> 
> 并行探索的隐含前提是：各子代理的任务之间不能有信息依赖。电商案例中，auth/db/api 三个子代理分别发现了 JWT 中的 role、users 表中的 role 字段、以及 /admin/* 路由的 role 检查，但因并行执行互不知情，role 的完整传递路径无法被任何单个子代理还原——跨模块的"连点成线"必须由主对话承担。
> 
> #### Q2 — 只读工具与 Haiku 模型选择
> 
> 只读工具（Read/Grep/Glob）确保探索子代理不会意外修改代码，职责边界清晰；选用 haiku 是因为探索任务相对简单，haiku 速度更快、成本更低，符合"追求速度"的工程目标。
> 
> #### Q3 — 并行探索与流水线编排对比
> 
> 并行探索适用于需要同时从多个独立角度理解一件事的场景（如拆解大型项目各模块），各子代理互不依赖；流水线编排适用于复杂任务可拆成多个连续阶段的场景（如修复 bug 时搜索→分析→修复→验证），前一阶段的输出是下一阶段的输入。
