---
tags: [geektime, ai-agents, openclaw, retry, fault-tolerance, production]
source: https://time.geekbang.org/column/article/957323
---

## Pre-test

> *阅读前尝试回答以下问题。答错完全正常——预测试能让大脑在接触正确答案时编码得更深。*

1. 在生产级 Agent 系统中，如果 LLM 的 API 触发了限流（HTTP 429），你认为系统应该怎么处理？直接返回失败，还是有其他方式恢复？
2. 如果一个 Agent 的对话历史太长、超出了模型的 Token 窗口上限，你认为有哪些方法可以解决这个问题？
3. 为什么配置多个 API Key（Profile）会提升 Agent 系统的稳定性？你能猜到它和重试次数上限有什么关系吗？

---

# 05｜核心重试循环与七重容错策略：生产级 Agent 系统如何实现渐进式降级？

## 章节元数据
- 课程名（EN）: geektime-selected-articles-20260327
- 课程名（ZH）: 极客时间精选文章
- 章节: 
- 作者: Henry
- 文章 ID: 957323
- 来源: https://time.geekbang.org/column/article/957323

## 康奈尔笔记

### 线索栏（问题）
- 本章的核心论点是什么？
- 本章开场提出了哪些背景、问题与动机？
- “主题2”这一部分的关键观点是什么？
- “主题2”有哪些支撑证据或案例？
- “主题3”这一部分的关键观点是什么？
- “主题3”有哪些支撑证据或案例？
- “七重容错策略详解”这一部分的关键观点是什么？
- “七重容错策略详解”有哪些支撑证据或案例？

### 笔记栏
#### 概览
- 你好，我是 Henry，欢迎来到《OpenClaw核心原理与实战》。
- 凌晨三点，你的手机突然响起告警：生产环境的Agent服务大面积报错。你睡眼惺忪地打开监控面板，发现是LLM供应商的API触发了限流——每分钟请求数超过了配额上限。更糟糕的是，由于没有容错机制，所有正在执行的Agent任务都直接失败了，用户看到的是一片“服务暂时不可用”的错误提示。
- 这个场景是否似曾相识？在生产环境中，Agent系统面临的挑战远比开发环境复杂：网络抖动、API限流、Token超限、模型过载……任何一个环节出问题，都可能导致整个任务失败。一个没有容错能力的Agent，就像一个不会游泳的人被扔进大海——遇到第一个浪头就会被淹没。
- 今天这节课，我们将深入OpenClaw的 run.ts 文件，拆解它的核心重试循环和七重容错策略。你将学到：重试循环的骨架是什么样的？七种容错策略分别解决什么问题？为什么它们要按特定顺序排列？理解这些设计，你就能打造出“打不死的小强”般健壮的Agent系统。

#### 主题2
- 原始小节标题：while 循环：Agent Loop 的“心脏起搏器”
- 在上一课中，我们了解了Agent Loop的三层架构，其中最外层的 run.ts 负责重试与容错。现在让我们打开这个文件，看看它的核心结构。
- run.ts 的核心是一个while循环，可以将其理解为Agent的“心脏起搏器”——只要任务没有完成或没有遇到不可恢复的错误，它就会持续跳动，驱动Agent不断尝试。
- 这个循环结构清晰地体现了外层的职责：它不关心LLM如何调用、Prompt如何构建，只关心两件事——成功了就返回，失败了就想办法恢复。

#### 主题3
- 原始小节标题：动态重试上限：与 Profile 数量挂钩
- 一个重要的设计细节是：重试次数上限不是固定的，而是动态计算的。OpenClaw使用以下公式：maxAttempts = 24 + profileCount × 8
- 其中 profileCount 是配置的API Key数量。这个值被限制在 [32, 160] 的区间内：
- 为什么要这样设计？因为更多的Profile意味着更多的“备用弹药”——当一个API Key被限流时，系统可以切换到另一个继续执行。Profile越多，系统的容错空间就越大，允许的重试次数自然也应该更多。
- 理解了循环骨架之后，关键问题来了：当 catch 块捕获到错误时，handleError 函数内部发生了什么？这就是七重容错策略的舞台。

#### 七重容错策略详解
- OpenClaw的容错策略设计遵循一个核心原则：代价递增。就像医院急救一样，医生会先尝试最简单、副作用最小的治疗方案，只有在无效时才会升级到更激进的手段。七种策略按照对系统影响从小到大排列，确保在解决问题的同时最小化“副作用”。

#### 主题5
- 原始小节标题：策略一：Runtime Auth 自动刷新
- 触发条件：检测到 isAuthAssistantError(error) 且系统配置了Runtime Auth（如OAuth Token）。
- 原理：许多LLM服务使用有过期时间的访问令牌。OpenClaw会在令牌过期前 5 分钟主动触发刷新，而不是等到过期后再处理。如果刷新正在进行中（refreshInFlight 为true），循环会等待刷新完成后自动重试。
- 代价：零成本。不丢失任何信息，不改变模型行为，仅产生极短的等待延迟。这就是为什么它排在第一位。

#### 策略二：上下文压缩
- 触发条件：检测到 isLikelyContextOverflowError(error)（上下文超出模型窗口限制）且压缩尝试次数小于3次。
- 原理：当对话历史过长导致Token超限时，OpenClaw会调用可插拔的上下文引擎（contextEngine.compact()）对历史记录进行摘要化处理。这个引擎可以使用LLM来提取关键信息，将冗长的对话压缩成精炼的摘要。
- 代价：信息损失。旧的对话细节会被模糊化，Agent可能“忘记”一些早期的上下文。但核心语义得以保留，任务通常可以继续。上限设为3次是为了防止无限压缩导致信息损失过多。

#### 策略三：工具结果截断
- 触发条件：上下文压缩达到3次上限后仍然溢出，且 sessionLikelyHasOversizedToolResults(session) 返回true。
- 原理：有时候上下文溢出不是因为对话历史太长，而是因为某个工具返回了巨量数据——比如Agent读取了一个10MB的日志文件。这种情况下，OpenClaw会直接截断该工具的输出，保留头部和尾部内容，中间标记为 [truncated]。
- 代价：更多信息损失。比压缩更激进，直接丢弃了部分工具执行的原始数据。但这是在“任务失败”和“部分信息丢失”之间的权衡——通常后者更可接受。

#### 主题8
- 原始小节标题：策略四：Profile 轮换
- 触发条件：检测到 isAuthOrRateLimitError(error)（认证失败或HTTP 429限流错误）。
- 原理：当一个API Key被限流或失效时，OpenClaw会标记该Profile为“暂时不可用”，然后自动切换到候选列表中的下一个可用Profile。这就是为什么配置多个API Key是生产环境的最佳实践。
- 代价：资源切换。需要消耗备用的API额度，但对用户体验无功能性影响。这是一个“花钱买稳定”的策略。

### 总结
构建“打不死”的 Agent 系统。

## 关键要点
- 你好，我是 Henry，欢迎来到《OpenClaw核心原理与实战》。
- 主题2：在上一课中，我们了解了Agent Loop的三层架构，其中最外层的 run.ts 负责重试与容错。现在让我们打开这个文件，看看它的核心结构。
- 主题3：一个重要的设计细节是：重试次数上限不是固定的，而是动态计算的。OpenClaw使用以下公式：maxAttempts = 24 + profileCount × 8
- 七重容错策略详解：OpenClaw的容错策略设计遵循一个核心原则：代价递增。就像医院急救一样，医生会先尝试最简单、副作用最小的治疗方案，只有在无效时才会升级到更激进的手段。七种策略按照对系统影响从小到大排列，确保在解决问题的同时最小化“副作用”。
- 主题5：触发条件：检测到 isAuthAssistantError(error) 且系统配置了Runtime Auth（如OAuth Token）。
- 策略二：上下文压缩：触发条件：检测到 isLikelyContextOverflowError(error)（上下文超出模型窗口限制）且压缩尝试次数小于3次。

## 知识图谱种子
- 实体:
  - geektime-selected-articles-20260327
  - 05｜核心重试循环与七重容错策略：生产级 Agent 系统如何实现渐进式降级？
  - Henry
- 关系:
  - (geektime-selected-articles-20260327) -> 包含章节 -> (05｜核心重试循环与七重容错策略：生产级 Agent 系统如何实现渐进式降级？)
  - (05｜核心重试循环与七重容错策略：生产级 Agent 系统如何实现渐进式降级？) -> 作者 -> (Henry)

## 复习备注
- 构建知识图谱前，先复核关键论断与原文的一致性。
- 在此补充你的行动项、实践映射和复盘结论。


---

## Post-test

> *关闭文件，凭记忆写出或大声说出你的答案，再对照答案指南（费曼检验：无法简单解释，说明仍有理解空白）。*

1. 用自己的话解释 OpenClaw 动态重试上限的计算公式，并说明为什么要把 profileCount 纳入计算，而不是设置一个固定数字。
2. OpenClaw 的七重容错策略遵循"代价递增"原则。请用急救类比说明这个原则的含义，并依序描述前四种策略分别解决什么问题、代价各是什么。
3. 策略二（上下文压缩）为什么要设置"最多压缩 3 次"的上限？压缩次数用完后系统会转向哪种策略，两者在信息损失程度上有何区别？

> [!example]- Answer Guide
> 
> #### Q1 — Dynamic Retry Limit Formula
> 
> 公式为 `maxAttempts = 24 + profileCount × 8`，区间限制在 [32, 160]。因为 Profile 越多意味着备用 API Key 越多，系统容错空间越大，允许更多重试才合理；固定数字无法反映实际可用的"备用弹药"数量。
> 
> #### Q2 — Cost-Escalating Fault Tolerance Strategies
> 
> 就像急救时医生先用副作用最小的方案、无效再升级：策略一是 Runtime Auth 自动刷新（零成本，仅短暂等待）；策略二是上下文压缩（代价：旧对话细节模糊化）；策略三是工具结果截断（代价：直接丢弃部分原始数据）；策略四是 Profile 轮换（代价：消耗备用 API 额度，但对用户无功能性影响）。
> 
> #### Q3 — Compression Limit and Fallback
> 
> 上限 3 次是为防止无限压缩导致信息损失过多。3 次用尽后系统转向工具结果截断——压缩是摘要化保留核心语义，截断则是直接丢弃中间原始数据，信息损失比压缩更激进。
