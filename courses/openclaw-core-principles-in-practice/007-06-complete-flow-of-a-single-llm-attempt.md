---
tags: [geektime, ai-agents, openclaw, llm, tools, workflow]
source: https://time.geekbang.org/column/article/957339
---

## Pre-test

> *阅读前尝试回答以下问题。答错完全正常——预测试能让大脑在接触正确答案时编码得更深。*

1. 在 Agent 执行任何任务之前，系统需要先回答什么根本性问题？你认为"工具注册"这一步发生在什么时机，为什么它必须是第一步？
2. 如果一个多租户 Agent 平台需要控制"不同用户能使用哪些工具"，你会如何设计这个权限控制机制？
3. 当 LLM 调用一个工具出现错误时，你认为哪类错误是"可以让 LLM 重试修复的"，哪类是"无论如何都不应绕过的"？两者的本质区别是什么？

---

# 06｜单次 LLM 尝试的完整流程：从工具注册开始发生了什么？

## 章节元数据
- 课程名（EN）: geektime-selected-articles-20260327
- 课程名（ZH）: 极客时间精选文章
- 章节: 
- 作者: Henry
- 文章 ID: 957339
- 来源: https://time.geekbang.org/column/article/957339

## 康奈尔笔记

### 线索栏（问题）
- 本章的核心论点是什么？
- 本章开场提出了哪些背景、问题与动机？
- “为什么工具注册是第一步？”这一部分的关键观点是什么？
- “为什么工具注册是第一步？”有哪些支撑证据或案例？
- “主题3”这一部分的关键观点是什么？
- “主题3”有哪些支撑证据或案例？
- “工具注册工厂：20+ 种能力的集中注册”这一部分的关键观点是什么？
- “工具注册工厂：20+ 种能力的集中注册”有哪些支撑证据或案例？

### 笔记栏
#### 概览
- 你好，我是 Henry，欢迎来到《OpenClaw核心原理与实战》。
- 当你按下发送按钮，看到Agent开始思考时，你是否想过：在LLM真正收到你的消息之前，系统究竟做了多少准备工作？
- 如果把Agent Loop比作一次火箭发射，那么我们前两课讲的重试循环就是“发射控制中心”——它负责决定何时发射、失败后如何重试。而今天我们要深入的 attempt.ts，则是“发射前的检查清单”——从燃料加注到姿态校准，从通信测试到倒计时确认，每一个步骤都必须精确无误，火箭才能顺利升空。
- 在OpenClaw中，一次单次LLM尝试（Attempt）涉及的准备工作之复杂，可能会超出你的想象：工具注册、策略过滤、Prompt构建、历史清理、Provider适配……这条准备链条包含了2923行精心设计的代码。今天，我们就来逐一拆解这条链条的每一个环节，看看OpenClaw是如何确保每一次LLM调用都“弹无虚发”的。

#### 为什么工具注册是第一步？
- 在Agent能够执行任何任务之前，系统必须首先回答一个根本性问题：这个 Agent 能做什么？
- 工具注册就是定义Agent能力边界的过程。一个没有注册任何工具的Agent，就像一个只会说话但没有手脚的人——它可以分析问题、给出建议，但无法执行任何实际操作。而一个注册了文件读写、代码执行、网页搜索等工具的Agent，则具备了知行合一的能力。

#### 主题3
- 原始小节标题：Sandbox 环境解析：建立安全边界
- 在注册工具之前，OpenClaw首先通过 resolveUserPath() 函数解析工作目录，建立Sandbox安全边界。这一步至关重要——它决定了Agent能够访问的文件系统范围。
- 通过这种配置，Agent只能在指定的工作目录下读写文件，无法访问系统敏感路径。这就像给Agent画了一个活动范围——它可以在自己的“房间”里自由活动，但不能擅自进入其他“禁区”。

#### 工具注册工厂：20+ 种能力的集中注册
- OpenClaw通过 createOpenClawCodingTools() 工厂函数集中注册所有可用工具。这些工具按功能分为多个类别：
- 这种集中注册的设计模式带来了两个好处：一是所有工具定义集中管理，便于维护和审计；二是支持根据场景动态调整工具集——比如在“只读模式”下自动排除所有写入类工具。

#### 工具类型体系与错误分类
- OpenClaw定义了统一的工具类型 AnyAgentTool，所有工具必须遵循这个接口规范。更重要的是，系统对工具执行错误进行了精细分类。
- 这种分类的意义在于：
- 400 错误是可修复的——LLM可以根据错误信息调整参数重新调用。
- 而403 错误是不可逾越的——无论LLM如何尝试，都不应该绕过权限限制。

#### 为什么需要多级策略？
- 想象这样一个场景：你运营一个多租户的Agent平台，不同租户有不同的权限等级，同一个租户的不同Agent也有不同的能力范围。如何灵活地控制“谁能用什么工具”？
- OpenClaw的答案是责任链模式的策略管道——工具必须依次通过多级关卡的审核，全部通过后才能出现在LLM的可用工具列表中。

#### 七级策略过滤详解
- 策略管道包含七级过滤，按优先级从高到低依次执行：

#### 主题8
- 原始小节标题：前置过滤与 Schema 归一化
- 在进入七级管道之前，系统还会进行一轮“前置过滤”。
- 消息通道过滤：在语音通道（voice）中禁用TTS工具，避免重复朗读。
- 模型能力过滤：如果模型内置了搜索能力（如某些版本的Claude），则自动禁用自定义的 web_search 工具，避免功能冲突。
- 权限标记检查：标记为 Owner-Only 的工具（如 exec）只有会话所有者才能使用。
- Schema 归一化：不同Provider对工具定义的JSON Schema格式有细微差异（比如Gemini要求特定的字段顺序）。归一化过程会自动修复这些格式问题，确保工具定义能被所有Provider正确解析。
- 策略管道确保了工具的“可用性”，接下来我们需要告诉LLM如何使用这些工具——这就是System Prompt的职责。

### 总结
看看 OpenClaw 是如何确保每一次 LLM 调用都“弹无虚发”的。

## 关键要点
- 你好，我是 Henry，欢迎来到《OpenClaw核心原理与实战》。
- 为什么工具注册是第一步？：在Agent能够执行任何任务之前，系统必须首先回答一个根本性问题：这个 Agent 能做什么？
- 主题3：在注册工具之前，OpenClaw首先通过 resolveUserPath() 函数解析工作目录，建立Sandbox安全边界。这一步至关重要——它决定了Agent能够访问的文件系统范围。
- 工具注册工厂：20+ 种能力的集中注册：OpenClaw通过 createOpenClawCodingTools() 工厂函数集中注册所有可用工具。这些工具按功能分为多个类别：
- 工具类型体系与错误分类：OpenClaw定义了统一的工具类型 AnyAgentTool，所有工具必须遵循这个接口规范。更重要的是，系统对工具执行错误进行了精细分类。
- 为什么需要多级策略？：想象这样一个场景：你运营一个多租户的Agent平台，不同租户有不同的权限等级，同一个租户的不同Agent也有不同的能力范围。如何灵活地控制“谁能用什么工具”？

## 知识图谱种子
- 实体:
  - geektime-selected-articles-20260327
  - 06｜单次 LLM 尝试的完整流程：从工具注册开始发生了什么？
  - Henry
- 关系:
  - (geektime-selected-articles-20260327) -> 包含章节 -> (06｜单次 LLM 尝试的完整流程：从工具注册开始发生了什么？)
  - (06｜单次 LLM 尝试的完整流程：从工具注册开始发生了什么？) -> 作者 -> (Henry)

## 复习备注
- 构建知识图谱前，先复核关键论断与原文的一致性。
- 在此补充你的行动项、实践映射和复盘结论。


---

## Post-test

> *关闭文件，凭记忆写出或大声说出你的答案，再对照答案指南（费曼检验：无法简单解释，说明仍有理解空白）。*

1. 用自己的话解释 OpenClaw 中 Sandbox 安全边界是如何建立的，`resolveUserPath()` 函数的作用是什么，这样设计解决了什么问题？
2. `createOpenClawCodingTools()` 工厂函数的集中注册模式带来了哪两个具体好处？请举例说明"只读模式"下这种设计如何发挥作用。
3. OpenClaw 的七级策略过滤管道采用了什么设计模式？前置过滤阶段会处理哪些场景（至少说出三种），它与七级管道的关系是什么？

> [!example]- Answer Guide
> 
> #### Q1 — Sandbox Security Boundary via resolveUserPath
> 
> OpenClaw 通过 `resolveUserPath()` 解析工作目录，将 Agent 的文件系统访问范围限制在指定目录内，无法访问系统敏感路径。这相当于给 Agent 划定"活动范围"，是注册工具之前必须完成的安全边界建立步骤。
> 
> #### Q2 — Benefits of Centralized Tool Registration
> 
> 集中注册带来两个好处：一是所有工具定义集中管理，便于维护和审计；二是支持动态调整工具集，例如在"只读模式"下自动排除所有写入类工具，无需逐一配置。
> 
> #### Q3 — Seven-Stage Pipeline Design Pattern
> 
> 策略管道采用责任链模式，工具须依次通过七级关卡全部审核才能进入可用列表。前置过滤发生在七级管道之前，处理语音通道禁用 TTS 工具、模型内置能力冲突去重、Owner-Only 权限标记检查，以及不同 Provider 的 JSON Schema 格式归一化等场景。
